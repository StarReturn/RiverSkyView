# 第二期实施进度

> 项目：江天一览 RiverSkyView  
> 启动日期：2026-07-15  
> 当前状态：代码、自动化验证与安装包已完成；等待安装后的人工 UI 验收

## 阶段状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| 0 | 基线确认与保护 | 已完成 |
| 1 | P0 体验整改、品牌收敛与数据领域模型 | 已完成 |
| 2 | 内置编辑器适配器与安装检测 | 已完成 |
| 3 | 项目目标发现 | 已完成 |
| 4 | 安全启动与自定义配置 | 已完成 |
| 5 | Tauri 命令与前端 API | 已完成 |
| 6 | 设置页 | 已完成 |
| 7 | 项目列表与详情集成 | 已完成 |
| 8 | 自动化测试与质量整改 | 已完成 |
| 9 | Windows 实机验收 | 构建与数据库升级通过；安装后人工点击待执行 |
| 10 | 打包与交付 | 已完成 |

## 阶段 0 基线

- 项目目录：`D:\Project Documentation\Project_Manage`。
- 项目当前没有 Git 仓库；实施期间通过严格限定文件范围和测试结果保护现有工作。
- 未发现项目级 `AGENTS.md` 或 `CLAUDE.md` 额外约束。
- WP-2 品牌更名已经完成：江天一览 / RiverSkyView。
- 保留内部兼容标识：`project-manage`、`ProjectManager`、`.project-manager.json`、`com.projectmanage.app`。
- 基线前端测试：42 passed。
- 基线类型检查、Vite build、`cargo check`、Tauri NSIS build 均通过。
- 当前旧版进程占用默认 release EXE；后续发布构建使用 `src-tauri/target-v2` 独立目录。

## 实施记录

### 2026-07-15

- 正式启动第二期。
- 完成代码、数据库、路由、活动热力图、资料库与终端启动基线审查。
- 开始阶段 1：P0 界面问题、收藏入口合并和编辑器数据模型。
- 修复热力图隐藏标签页零宽初始化，改为完整 365 天数据和 `ResizeObserver` 自适应。
- 修复空状态按钮图标继承 48px 全局规则造成的畸变。
- 修复资料库“按住显示”，覆盖 Pointer Events、键盘、窗口失焦和异步解密竞态。
- 移除收藏独立侧栏入口，保留 `/favorites` 兼容跳转并统一到项目页筛选。
- 新增 `0002_editor_preferences.sql`，现有 1.0 数据库在线备份副本升级成功，项目数量保持不变。
- 完成编辑器检测、目标解析、安全启动、自定义配置、全局默认与项目默认。
- 项目列表和详情页已接入共享编辑器分裂按钮；设置页已接入编辑器管理。
- 质量结果：前端 46 passed；Rust 67 passed；lint/typecheck/build/fmt/clippy 全部通过。
- 生成未签名 NSIS：`src-tauri/target-brand/release/bundle/nsis/江天一览_1.1.0_x64-setup.exe`。
- 安装包 SHA-256：`DFC2907EF2A87AEAA117B995D763DDEA272A48E8EB0D078E93E1E2B72893DF87`。

## 待人工验收

- 安装 1.1.0 后，在真实项目中分别点击 VS Code、Cursor、Visual Studio/Rider。
- 使用含多个 `.sln` 的项目确认目标选择与记忆。
- 在 100%/125%/150% 缩放下复核热力图和操作按钮。
- 按住、拖出、松开服务器资料显示按钮，确认始终恢复遮罩。
